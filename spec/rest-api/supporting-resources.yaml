# SPDX-License-Identifier: AGPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 Greenbone AG

openapi: 3.1.0

paths:
  /hosts:
    get:
      operationId: getHosts
      tags: [Hosts]
      summary: List hosts
      description: Returns a paginated list of discovered hosts/assets.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of discovered hosts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HostList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
    post:
      operationId: createHost
      tags: [Hosts]
      summary: Create a host
      description: Creates a host asset identified by a name or IP address.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHost"
      responses:
        "201":
          description: Host created
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /hosts/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getHost
      tags: [Hosts]
      summary: Get a host
      description: Returns the details for a single discovered host/asset.
      responses:
        "200":
          description: Host details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Host"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifyHost
      tags: [Hosts]
      summary: Modify a host
      description: >-
        Updates a host asset's comment. The gvmd `modify_asset` command does not
        change a host asset's name/IP value, so only the comment can be edited
        here.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHost"
      responses:
        "200":
          description: Host updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Host"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteHost
      tags: [Hosts]
      summary: Delete a host
      description: >-
        Deletes a host asset. The gvmd host-asset delete command does not
        support the `ultimate` (permanent) flag, so this endpoint always
        performs a single delete.
      responses:
        "204":
          description: Host deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /operating-systems:
    get:
      operationId: getOperatingSystems
      tags: [Operating Systems]
      summary: List operating systems
      description: Returns a paginated list of operating-system assets.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of operating-system assets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatingSystemList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /operating-systems/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getOperatingSystem
      tags: [Operating Systems]
      summary: Get an operating system
      description: Returns the details for a single operating-system asset.
      responses:
        "200":
          description: Operating-system details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatingSystem"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifyOperatingSystem
      tags: [Operating Systems]
      summary: Modify an operating system
      description: >-
        Updates an operating-system asset's comment. The typed gvmd command only
        supports comment updates for this resource.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOperatingSystem"
      responses:
        "200":
          description: Operating-system updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatingSystem"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteOperatingSystem
      tags: [Operating Systems]
      summary: Delete an operating system
      description: >-
        Deletes an operating-system asset. The typed gvmd delete command does
        not support the `ultimate` flag for this resource, so this endpoint
        always performs a single delete. Deleting an asset that is still in use
        returns `409 Conflict`.
      responses:
        "204":
          description: Operating-system deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
        "409":
          $ref: "./common.yaml#/components/responses/Conflict"

  /tls-certificates:
    get:
      operationId: getTlsCertificates
      tags: [TLS Certificates]
      summary: List TLS certificates
      description: Returns a paginated list of TLS certificate assets.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of TLS certificate assets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TlsCertificateAssetList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /tls-certificates/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getTlsCertificate
      tags: [TLS Certificates]
      summary: Get a TLS certificate
      description: Returns the details for a single TLS certificate asset.
      responses:
        "200":
          description: TLS certificate details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TlsCertificateAsset"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /report-formats:
    get:
      operationId: getReportFormats
      tags: [Report Formats]
      summary: List report formats
      description: Returns a paginated list of report formats available for report export.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of report formats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportFormatList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /report-formats/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getReportFormat
      tags: [Report Formats]
      summary: Get a report format
      description: Returns the details for a single report format.
      responses:
        "200":
          description: Report format details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportFormat"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /filters:
    get:
      operationId: getFilters
      tags: [Filters]
      summary: List filters
      description: Returns a paginated list of saved filters.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of saved filters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FilterList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
    post:
      operationId: createFilter
      tags: [Filters]
      summary: Create a filter
      description: Creates a saved filter with an optional term, comment, and resource type.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFilter"
      responses:
        "201":
          description: Filter created
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /filters/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getFilter
      tags: [Filters]
      summary: Get a filter
      description: Returns the details for a single saved filter.
      responses:
        "200":
          description: Filter details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Filter"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifyFilter
      tags: [Filters]
      summary: Modify a filter
      description: Updates a saved filter's term, comment, or resource type.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFilter"
      responses:
        "200":
          description: Filter updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Filter"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteFilter
      tags: [Filters]
      summary: Delete a filter
      description: Deletes a saved filter. Pass `ultimate=true` to request permanent backend deletion instead of the default non-ultimate delete.
      parameters:
        - $ref: "./common.yaml#/components/parameters/UltimateDelete"
      responses:
        "204":
          description: Filter deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /filters/{id}/clone:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    post:
      operationId: cloneFilter
      tags: [Filters]
      summary: Clone a filter
      description: Creates a copy of an existing saved filter.
      responses:
        "201":
          description: Filter cloned
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /tags:
    get:
      operationId: getTags
      tags: [Tags]
      summary: List tags
      description: Returns a paginated list of tags.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of tags
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
    post:
      operationId: createTag
      tags: [Tags]
      summary: Create a tag
      description: Creates a tag, optionally attaching it to a related resource by type and id.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTag"
      responses:
        "201":
          description: Tag created
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /tags/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getTag
      tags: [Tags]
      summary: Get a tag
      description: Returns the details for a single tag.
      responses:
        "200":
          description: Tag details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResource"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifyTag
      tags: [Tags]
      summary: Modify a tag
      description: Updates a tag's value, comment, resource attachment, or active state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTag"
      responses:
        "200":
          description: Tag updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResource"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteTag
      tags: [Tags]
      summary: Delete a tag
      description: Deletes a tag. Pass `ultimate=true` to request permanent backend deletion instead of the default non-ultimate delete.
      parameters:
        - $ref: "./common.yaml#/components/parameters/UltimateDelete"
      responses:
        "204":
          description: Tag deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /tags/{id}/clone:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    post:
      operationId: cloneTag
      tags: [Tags]
      summary: Clone a tag
      description: Creates a copy of an existing tag.
      responses:
        "201":
          description: Tag cloned
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /notes:
    get:
      operationId: getNotes
      tags: [Notes]
      summary: List notes
      description: Returns a paginated list of notes that annotate findings. Filter expressions can scope notes to the related task, result, NVT, host, or port selectors exposed by each note.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of notes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
    post:
      operationId: createNote
      tags: [Notes]
      summary: Create a note
      description: Creates a note that annotates findings for the selected NVT and optional task/result/host/port/severity scope.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNote"
      responses:
        "201":
          description: Note created
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /notes/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getNote
      tags: [Notes]
      summary: Get a note
      description: Returns the details for a single note, including any related task/result identifiers and the NVT/host/port selectors the note annotates.
      responses:
        "200":
          description: Note details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifyNote
      tags: [Notes]
      summary: Modify a note
      description: Updates a note used for finding triage.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateNote"
      responses:
        "200":
          description: Note updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteNote
      tags: [Notes]
      summary: Delete a note
      description: Deletes a note. Pass `ultimate=true` to request permanent backend deletion instead of the default non-ultimate delete.
      parameters:
        - $ref: "./common.yaml#/components/parameters/UltimateDelete"
      responses:
        "204":
          description: Note deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /overrides:
    get:
      operationId: getOverrides
      tags: [Overrides]
      summary: List overrides
      description: Returns a paginated list of overrides that change finding interpretation. Filter expressions can scope overrides to the related task, result, NVT, host, or port selectors exposed by each override.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of overrides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OverrideList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
    post:
      operationId: createOverride
      tags: [Overrides]
      summary: Create an override
      description: Creates an override that changes finding interpretation for the selected NVT and optional task/result/host/port/severity scope.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOverride"
      responses:
        "201":
          description: Override created
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /overrides/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getOverride
      tags: [Overrides]
      summary: Get an override
      description: Returns the details for a single override, including any related task/result identifiers, the annotated NVT/host/port selectors, and the replacement severity when one is set.
      responses:
        "200":
          description: Override details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Override"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifyOverride
      tags: [Overrides]
      summary: Modify an override
      description: Updates an override used for finding triage.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOverride"
      responses:
        "200":
          description: Override updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Override"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteOverride
      tags: [Overrides]
      summary: Delete an override
      description: Deletes an override. Pass `ultimate=true` to request permanent backend deletion instead of the default non-ultimate delete.
      parameters:
        - $ref: "./common.yaml#/components/parameters/UltimateDelete"
      responses:
        "204":
          description: Override deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /vulnerabilities:
    get:
      operationId: getVulnerabilities
      tags: [Vulnerabilities]
      summary: List vulnerabilities
      description: Returns a paginated list of vulnerabilities from the SecInfo database.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of vulnerabilities
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VulnerabilityList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /cves:
    get:
      operationId: getCves
      tags: [CVEs]
      summary: List CVEs
      description: Returns a paginated list of CVE entries from the SecInfo database.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of CVEs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CveList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /cves/{id}:
    parameters:
      - $ref: "#/components/parameters/SecInfoId"
    get:
      operationId: getCve
      tags: [CVEs]
      summary: Get a CVE
      description: Returns the details for a single CVE entry.
      responses:
        "200":
          description: CVE details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cve"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /cpes:
    get:
      operationId: getCpes
      tags: [CPEs]
      summary: List CPEs
      description: Returns a paginated list of CPE entries from the SecInfo database.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of CPEs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CpeList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /cpes/{id}:
    parameters:
      - $ref: "#/components/parameters/SecInfoId"
    get:
      operationId: getCpe
      tags: [CPEs]
      summary: Get a CPE
      description: Returns the details for a single CPE entry.
      responses:
        "200":
          description: CPE details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cpe"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /cert-bund-advisories:
    get:
      operationId: getCertBundAdvisories
      tags: [CERT-Bund Advisories]
      summary: List CERT-Bund advisories
      description: Returns a paginated list of CERT-Bund advisory entries from the SecInfo database.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of CERT-Bund advisories
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CertBundAdvisoryList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /cert-bund-advisories/{id}:
    parameters:
      - $ref: "#/components/parameters/SecInfoId"
    get:
      operationId: getCertBundAdvisory
      tags: [CERT-Bund Advisories]
      summary: Get a CERT-Bund advisory
      description: Returns the details for a single CERT-Bund advisory entry.
      responses:
        "200":
          description: CERT-Bund advisory details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CertBundAdvisory"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /dfn-cert-advisories:
    get:
      operationId: getDfnCertAdvisories
      tags: [DFN-CERT Advisories]
      summary: List DFN-CERT advisories
      description: Returns a paginated list of DFN-CERT advisory entries from the SecInfo database.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of DFN-CERT advisories
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DfnCertAdvisoryList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /dfn-cert-advisories/{id}:
    parameters:
      - $ref: "#/components/parameters/SecInfoId"
    get:
      operationId: getDfnCertAdvisory
      tags: [DFN-CERT Advisories]
      summary: Get a DFN-CERT advisory
      description: Returns the details for a single DFN-CERT advisory entry.
      responses:
        "200":
          description: DFN-CERT advisory details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DfnCertAdvisory"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /nvts:
    get:
      operationId: getNvts
      tags: [NVTs]
      summary: List NVTs
      description: Returns a paginated list of network vulnerability tests available in the feed catalog.
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
        - name: configId
          in: query
          required: false
          description: Restrict the catalog to NVTs selected by this scan config.
          schema:
            type: string
            format: uuid
        - name: preferencesConfigId
          in: query
          required: false
          description: Resolve preference values from this scan config.
          schema:
            type: string
            format: uuid
        - name: family
          in: query
          required: false
          description: Restrict the catalog to one NVT family.
          schema:
            type: string
        - name: includePreferences
          in: query
          required: false
          description: Include NVT preference definitions and values.
          schema:
            type: boolean
        - name: includePreferenceCount
          in: query
          required: false
          description: Include each NVT's preference count.
          schema:
            type: boolean
        - name: includeTimeout
          in: query
          required: false
          description: Include the special timeout preference. When true, `configId` is required.
          schema:
            type: boolean
        - name: sortOrder
          in: query
          required: false
          description: Backend sort direction.
          schema:
            type: string
            enum: [ascending, descending]
        - name: sortField
          in: query
          required: false
          description: Backend NVT field used for sorting.
          schema:
            type: string
      responses:
        "200":
          description: Paginated list of NVTs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NvtList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

  /nvts/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: NVT OID
        schema:
          type: string
    get:
      operationId: getNvt
      tags: [NVTs]
      summary: Get an NVT
      description: Returns the details for a single network vulnerability test by OID.
      responses:
        "200":
          description: NVT details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Nvt"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

  /nvt-families:
    get:
      operationId: getNvtFamilies
      tags: [NVT Families]
      summary: List NVT families
      description: Returns a paginated list of NVT families. This endpoint is collection-only and does not accept filter expressions.
      parameters:
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of NVT families
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NvtFamilyList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "502":
          $ref: "./common.yaml#/components/responses/BadGateway"

components:
  parameters:
    SecInfoId:
      name: id
      in: path
      required: true
      schema:
        type: string
      description: SecInfo identifier such as a CVE, CPE, or advisory id

  schemas:
    Host:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        ip:
          type: string
        hostname:
          type: string
        severity:
          type: string
        os:
          type: string

    HostList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Host"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    OperatingSystemHost:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        severity:
          type: string

    OperatingSystem:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
        - title
        - installs
        - allInstalls
        - hostCount
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        value:
          type: string
        hostsCount:
          type: integer
        severity:
          type: string
        title:
          type: string
        installs:
          type: integer
        allInstalls:
          type: integer
        latestSeverity:
          type: string
        highestSeverity:
          type: string
        averageSeverity:
          type: string
        hostCount:
          type: integer
        hosts:
          type: array
          items:
            $ref: "#/components/schemas/OperatingSystemHost"

    OperatingSystemList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/OperatingSystem"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    TlsCertificateAsset:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
        - valid
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        subjectDn:
          type: string
        issuerDn:
          type: string
        activationTime:
          type: string
        expirationTime:
          type: string
        md5Fingerprint:
          type: string
        sha256Fingerprint:
          type: string
        certificate:
          type: string
        valid:
          type: boolean

    TlsCertificateAssetList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/TlsCertificateAsset"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    CreateHost:
      type: object
      required:
        - value
      properties:
        value:
          type: string
        comment:
          type: string

    UpdateHost:
      type: object
      properties:
        comment:
          type: string

    UpdateOperatingSystem:
      type: object
      properties:
        comment:
          type: string

    SupportingResourceMeta:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean

    ReportFormat:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
        - active
        - predefined
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        contentType:
          type: string
        extension:
          type: string
        summary:
          type: string
        trust:
          type: string
        active:
          type: boolean
        predefined:
          type: boolean

    ReportFormatList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ReportFormat"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    Filter:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        type:
          type: string
        term:
          type: string

    FilterList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Filter"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    CreateFilter:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        comment:
          type: string
        term:
          type: string
        type:
          type: string

    UpdateFilter:
      type: object
      properties:
        comment:
          type: string
        term:
          type: string
        type:
          type: string

    TagResource:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
        - active
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        value:
          type: string
        resourceType:
          type: string
        resourceCount:
          type: integer
        active:
          type: boolean

    TagList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/TagResource"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    CreateTag:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        comment:
          type: string
        value:
          type: string
        resourceType:
          type: string
        resourceId:
          type: string
          format: uuid
        active:
          type: boolean

    UpdateTag:
      type: object
      properties:
        comment:
          type: string
        value:
          type: string
        resourceType:
          type: string
        resourceId:
          type: string
          format: uuid
        active:
          type: boolean

    Note:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
        - active
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        text:
          type: string
        nvt:
          $ref: "./results.yaml#/components/schemas/NvtRef"
        hosts:
          type: array
          items:
            type: string
        port:
          type: string
        severity:
          type: string
        task:
          $ref: "./common.yaml#/components/schemas/ResourceRef"
        result:
          $ref: "./common.yaml#/components/schemas/ResourceRef"
        active:
          type: boolean
        endTime:
          type: string

    NoteList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Note"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    CreateNote:
      type: object
      required:
        - nvtOid
      properties:
        nvtOid:
          type: string
        text:
          type: string
        hosts:
          type: array
          items:
            type: string
        port:
          type: string
        severity:
          type: string
        taskId:
          type: string
          format: uuid
        resultId:
          type: string
          format: uuid
        active:
          type: boolean
        orphan:
          type: boolean

    UpdateNote:
      type: object
      properties:
        text:
          type: string
        hosts:
          type: array
          items:
            type: string
          description: Host selector list. Omitted or null leaves existing host selectors unchanged; an empty array clears all host selectors.
        port:
          type: string
        severity:
          type: string
        taskId:
          type: string
          format: uuid
        resultId:
          type: string
          format: uuid
        active:
          type: boolean
        orphan:
          type: boolean

    Override:
      type: object
      required:
        - id
        - name
        - writable
        - inUse
        - active
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        creationTime:
          type: string
        modificationTime:
          type: string
        writable:
          type: boolean
        inUse:
          type: boolean
        text:
          type: string
        nvt:
          $ref: "./results.yaml#/components/schemas/NvtRef"
        hosts:
          type: array
          items:
            type: string
        port:
          type: string
        severity:
          type: string
        newSeverity:
          type: string
        task:
          $ref: "./common.yaml#/components/schemas/ResourceRef"
        result:
          $ref: "./common.yaml#/components/schemas/ResourceRef"
        active:
          type: boolean
        endTime:
          type: string

    OverrideList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Override"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    CreateOverride:
      type: object
      required:
        - nvtOid
      properties:
        nvtOid:
          type: string
        text:
          type: string
        hosts:
          type: array
          items:
            type: string
        port:
          type: string
        severity:
          type: string
        newSeverity:
          type: string
        taskId:
          type: string
          format: uuid
        resultId:
          type: string
          format: uuid
        active:
          type: boolean

    UpdateOverride:
      type: object
      properties:
        text:
          type: string
        hosts:
          type: array
          items:
            type: string
          description: Host selector list. Omitted or null leaves existing host selectors unchanged; an empty array clears all host selectors.
        port:
          type: string
        severity:
          type: string
        newSeverity:
          type: string
        taskId:
          type: string
          format: uuid
        resultId:
          type: string
          format: uuid
        active:
          type: boolean

    Nvt:
      type: object
      required:
        - oid
        - name
      properties:
        oid:
          type: string
        name:
          type: string
        family:
          type: string
        cvssBase:
          type: number
        severity:
          type: number
        tags:
          type: string
        solutionType:
          type: string

    NvtList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Nvt"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    Vulnerability:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string

    VulnerabilityList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Vulnerability"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    Cve:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string

    CveList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Cve"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    Cpe:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string

    CpeList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Cpe"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    CertBundAdvisory:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string

    CertBundAdvisoryList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/CertBundAdvisory"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    DfnCertAdvisory:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string

    DfnCertAdvisoryList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/DfnCertAdvisory"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    NvtFamily:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        maxNvtCount:
          type: integer

    NvtFamilyList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/NvtFamily"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"
